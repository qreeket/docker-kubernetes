# config for redis HA setup
redis:
  existingSecret: redis-credentials
  masterGroupName: qcodelabsllc

sentinel:
  auth: true
  existingSecret: redis-credentials

haproxy:
  enabled: true
  service:
    type: LoadBalancer
