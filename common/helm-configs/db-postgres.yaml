# config for postgres HA setup
volumePermissions:
  enabled: true

global:
  postgresql:
    existingSecret: postgres-credentials
  pgpool:
    existingSecret: postgres-credentials

service:
  type: LoadBalancer

backup:
  enabled: true
  cronjob:
    schedule: "0 0 * * *"
